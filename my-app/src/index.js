import ReactDOM from "react-dom";

import "./index.css";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
// what we tell here  that replace the root (in index.html) that selected by id
// with <App/> component.
